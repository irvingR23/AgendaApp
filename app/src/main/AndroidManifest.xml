<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dat.agendamedica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgendaMedica">
        <activity
            android:name=".detallesCita"
            android:label="@string/title_activity_detalles_cita"
            android:theme="@style/Theme.AgendaMedica.NoActionBar"></activity>
        <activity
            android:name=".VerCitasDetalladas"
            android:label="@string/title_activity_ver_citas_detalladas"
            android:theme="@style/Theme.AgendaMedica.NoActionBar" />
        <activity
            android:name=".CrearCita"
            android:label="@string/title_activity_crear_cita"
            android:theme="@style/Theme.AgendaMedica.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>